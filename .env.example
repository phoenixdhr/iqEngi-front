# Ejemplo de configuración de variables de entorno
# Copia este archivo según el entorno que necesites:
# - Para desarrollo local con backend local: usa .env.dev
# - Para desarrollo con backend de producción: usa .env (por defecto)
# - Para producción: usa .env.production

# GraphQL Backend URL
PUBLIC_GRAPHQL_URL = https://iqengi-backend-.....
PUBLIC_DOMAIN = http://localhost:4321

# testiqenginefront-production.up.railway.app

ENVIRONMENT = development (si es desarrollo .env.dev o .env)
ENVIRONMENT = production (si es producción .env.production)

# Resend API Key (para envío de emails)
# Obtén tu API key en: https://resend.com/api-keys
RESEND_API_KEY = re_xxxxxxxxxxxxxxxxxxxxxxxxxx

# Email de contacto (opcional - por defecto usa siteInfo.email)
# Este es el email donde llegarán los mensajes del formulario de contacto
CONTACT_EMAIL = contacto@tudominio.com