---

---

<!-- Contenedor principal del modal de login, oculto por defecto (clase "hidden") -->
<div
    id="forgot-password-modal"
    class="hidden fixed inset-0 bg-[rgba(0,0,0,0.85)] bg-opacity-80 flex items-center justify-center z-50"
>
    <!-- Caja interna del modal que contiene el formulario de inicio de sesión -->
    <div
        id="astro-forgot-password"
        class="bg-gray-100 text-gray-900 p-8 rounded-3xl w-full max-w-md h-[500px] relative shadow-2xl flex flex-col"
    >
        <!-- Botón de cierre del modal. Al hacer clic, agrega la clase "hidden" para ocultar el modal -->
        <button
            class="absolute top-4 right-4 text-gray-600 hover:text-gray-800 text-xl"
            onclick="document.getElementById('forgot-password-modal').classList.add('hidden')"
        >
            ✖
        </button>

        <!-- Flecha para ir atrás (o cerrar, según tu preferencia) -->
        <button
            class="absolute top-4 left-4 text-gray-600 hover:text-gray-800 text-2xl mr-4"
            onclick="document.getElementById('forgot-password-modal').classList.add('hidden')"
        >
            ←
        </button>

        <!-- Logo de IQ-ENGI (ejemplo) -->
        <div class="h-12"></div>

        <div class="flex items-center lg:flex-1 justify-center">
            <a href="#" class="flex items-center space-x-2">
                <img
                    class="h-8 lg:h-10 w-auto"
                    src="/favicon.svg"
                    alt="IqEngi"
                />
                <span
                    class="bg-gradient-to-br from-purple-600 to-blue-500 bg-clip-text text-transparent font-bold text-2xl lg:text-3xl"
                >
                    IQ-ENGI
                </span>
            </a>
        </div>
        <!-- Título del modal que indica la acción a realizar -->
        <h2 class="text-xl mb-6 text-center">Revisa tu email</h2>
        <p class="text-sm text-gray-500 mb-4">
            Te hemos enviado un correo electrónico a {`phoenixdhr@gmail.com`} con
            el enlace para que puedas crear una nueva contraseña. Si no te ha llegado
            en unos minutos, revisa la carpeta de los correos no deseados.
        </p>
        <!-- Contenedor del formulario de login -->
        <div
            id="astro-login-form-google"
            class="relative flex flex-col justify-center mt-15"
        >
            <!-- Botón de inicio de sesión alternativo usando la cuenta de Google -->
            <button
                id="btnContinuar"
                type="button"
                onclick="document.getElementById('forgot-password-modal').classList.add('hidden')"
                class="w-full h-12 text-white bg-gradient-to-br from-purple-600 to-blue-500 hover:bg-gradient-to-bl focus:ring-4 font-medium text-sm hover:scale-105 transition-transform rounded-full"
            >
                Volver al inicio →
            </button>

            <hr class="w-full border-t border-gray-300 my-8" />

            <span class="text-sm text-gray-500 mb-4">
                Si necesitas ayuda comunícate con nuestro equipo de soporte a
                team@iqengi.com.
            </span>
        </div>
    </div>
</div>

<!-- Script que escucha el evento 'open-forgot-password-modal' para mostrar el modal al remover la clase "hidden" -->
<script>
    // window.addEventListener('open-forgot-password-modal', () => {
    //     document
    //         .getElementById('forgot-password-modal')
    //         ?.classList.remove('hidden');
    // });
</script>
