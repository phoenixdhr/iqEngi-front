---
/* Si estás usando Astro, aquí podrías importar algo si fuera necesario,
   por ejemplo variables de entorno o configuración del sitio.
   En caso contrario, puedes omitir esta sección. */
---

<!-- Modal para "Crear una contraseña" -->
<div
    id="create-password-modal"
    class="hidden fixed inset-0 bg-[rgba(0,0,0,0.85)] bg-opacity-80 flex items-center justify-center z-[9999]"
>
    <div
        id="create-password-box"
        class="bg-[#0A0A0A] text-white p-6 rounded-2xl w-full max-w-sm h-auto relative shadow-2xl flex flex-col"
    >
        <!-- Botón para cerrar el modal (esquina superior derecha) -->
        <button
            class="absolute top-4 right-4 text-gray-400 hover:text-gray-200 text-xl"
            onclick="document.getElementById('create-password-modal').classList.add('hidden')"
        >
            ✖
        </button>

        <!-- Encabezado: flecha de retroceso y logo -->
        <div class="flex items-center mb-6">
            <!-- Flecha para ir atrás (o cerrar, según tu preferencia) -->
            <button
                class="text-gray-400 hover:text-gray-200 text-2xl mr-4"
                onclick="document.getElementById('create-password-modal').classList.add('hidden')"
            >
                ←
            </button>
            <!-- Logo de Platzi (ejemplo) -->
            <img
                src="https://static.platzi.com/media/platzi-isotipo@2x.png"
                alt="Platzi"
                class="h-6"
            />
        </div>

        <!-- Título e indicación -->
        <h2 class="text-xl font-semibold mb-1">Crea una contraseña</h2>
        <p class="text-sm text-gray-400 mb-4">
            Debe tener al menos 10 caracteres
        </p>

        <!-- Formulario para ingresar y confirmar contraseña -->
        <form class="space-y-4">
            <input
                type="password"
                placeholder="Contraseña"
                required
                class="w-full h-12 px-4 bg-[#121212] border border-gray-700 focus:outline-none focus:ring-2 focus:ring-green-400 rounded-full text-gray-100 placeholder:text-gray-400"
            />
            <input
                type="password"
                placeholder="Confirmar contraseña"
                required
                class="w-full h-12 px-4 bg-[#121212] border border-gray-700 focus:outline-none focus:ring-2 focus:ring-green-400 rounded-full text-gray-100 placeholder:text-gray-400"
            />

            <button
                type="submit"
                class="w-full h-12 text-white bg-gradient-to-br from-green-500 to-green-600 hover:bg-gradient-to-bl focus:ring-4 font-medium text-sm hover:scale-105 transition-transform rounded-full"
            >
                Continuar
            </button>
        </form>

        <!-- Aviso de términos y políticas -->
        <p class="text-xs text-gray-500 text-center mt-6">
            Al crear una cuenta en Platzi, aceptas los
            <a href="#" class="text-green-400 underline">Términos de Servicio</a
            > y
            <a href="#" class="text-green-400 underline"
                >Políticas de Privacidad</a
            >.
        </p>
    </div>
</div>

<!-- Listener para mostrar el modal cuando se dispare el evento "open-create-password-modal" -->
<script>
    window.addEventListener('open-create-password-modal', () => {
        document
            .getElementById('create-password-modal')
            ?.classList.remove('hidden');
    });
</script>
