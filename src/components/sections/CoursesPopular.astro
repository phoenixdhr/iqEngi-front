---
import type { Curso } from '@graphql-astro/generated/graphql';
import { PopularCoursesList } from '@components/features/PopularCoursesList';

interface Props {
  cursos: Curso[];
}

const { cursos } = Astro.props;
---

<PopularCoursesList client:visible initialCourses={cursos} />
